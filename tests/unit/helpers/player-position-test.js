import { playerPosition } from 'coach-assistant/helpers/player-position';
import { module, test } from 'qunit';

module('Unit | Helper | player position');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = playerPosition([42]);
  assert.ok(result);
});
